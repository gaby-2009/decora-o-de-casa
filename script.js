const searchInput = document.querySelector('input[name="q"]');
searchInput.addEventListener('input', function() {
  // Aqui você pode adicionar funcionalidades como autocomplete ou filtragem dinâmica
});
